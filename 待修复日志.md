# 缺失资源与待修复问题日志

> 生成日期: 2026-05-27
> 项目: MU Online 客户端 (Season 8 / EX502)

---

## 一、ExtTile 纹理全部缺失（严重）

**影响：** 地形扩展区域渲染为紫/黑色棋盘格，44 个地图全部受影响。这是「界面渲染不正常」的直接原因。

**源码位置：** `Source Client/source/MapManager.cpp:1543-1550`

```cpp
for(int i = 1; i <= 16; i++)
{
    if(i >= 10)
        sprintf (FileName,"%s\\ExtTile%d.jpg",WorldName, i);
    else
        sprintf (FileName,"%s\\ExtTile0%d.jpg",WorldName, i);
    LoadBitmap(FileName,BITMAP_MAPTILE+13+i ,GL_NEAREST,GL_REPEAT,false);
}
```

**缺失文件（44 个 World 目录 × 16 文件 = 704 个）：**

| 世界目录 | 缺失文件列表 |
|---------|------------|
| World1 ~ World80 | `ExtTile01.jpg` ~ `ExtTile16.jpg`（共 16 个） |

涉及的世界：World1, World2, World3, World4, World5, World7, World8, World9, World10, World11, World12, World19, World25, World31, World32, World34, World35, World38, World39, World40, World41, World42, World43, World47, World52, World57, World58, World59, World63, World64, World65, World66, World67, World68, World69, World70, World71, World72, World73, World74, World75, World78, World79, World80

---

## 二、Effect 纹理缺失（中等）

### 2.1 真缺失（文件不存在于任何目录）

| 引用位置 | 代码 | 文件 |
|---------|------|------|
| `ZzzOpenData.cpp:4454` | `LoadBitmap("Effect\\ground_smoke.tga", BITMAP_GROUND_SMOKE, ...)` | `Data\Effect\ground_smoke.OZT` |
| `ZzzOpenData.cpp:4448` | `LoadBitmap("Effect\\ground_wind.jpg", BITMAP_GROUND_WIND, ...)` | `Data\Effect\ground_wind.OZJ` |

### 2.2 跨目录引用（文件存在但路径错误）✅ 已修复

BMD 模型文件内嵌纹理名称，`OpenTexture()` 按指定 SubFolder 构造路径，但实际文件在另一目录。

**修复方式：** `GlobalBitmap.cpp:418-450` 自动跨目录搜索（Skill/Effect/Item/Interface/Player/Monster/NPC/Character）

| 请求路径 | 实际位置 | 引用方式 |
|---------|---------|---------|
| `Data\Effect\maskreta01.jpg` | `Data\Skill\maskreta01.OZJ` | BMD 内嵌 → `OpenTexture("Effect\\")` ✅ 自动 |
| `Data\Effect\maskreta03.tga` | `Data\Skill\maskreta03.OZT` | BMD 内嵌 → `OpenTexture("Effect\\")` ✅ 自动 |
| `Data\Effect\youmuk01aeff.jpg` | `Data\Skill\youmuk01aeff.OZJ` | BMD 内嵌 → `OpenTexture("Effect\\")` ✅ 自动 |
| `Data\Effect\sphere_item.jpg` | `Data\Item\sphere_item.OZJ` | BMD 内嵌 → `OpenTexture("Effect\\")` ✅ 自动 |

---

## 三、Skill 纹理缺失（中等）

### 3.1 真缺失

| 引用位置 | 代码 | 文件 |
|---------|------|------|
| BMD 内嵌 + `TextureScript.cpp:71` | `mu_rgb_lights.jpg` → m_bBright | `Data\Skill\mu_rgb_lights.OZJ` |
| BMD 内嵌 | `skeleton.jpg` | `Data\Skill\skeleton.OZJ` |

### 3.2 跨目录引用 ✅ 已修复

| 请求路径 | 实际位置 | 引用方式 |
|---------|---------|---------|
| `Data\Skill\panda_body.jpg` | `Data\Item\panda_body.OZJ` | BMD 内嵌 → `OpenTexture("Skill\\")` ✅ |
| `Data\Skill\panda_face.jpg` | `Data\Item\panda_face.OZJ` | BMD 内嵌 → `OpenTexture("Skill\\")` ✅ |
| `Data\Skill\panda_cloth.jpg` | `Data\Item\panda_cloth.OZJ` | BMD 内嵌 → `OpenTexture("Skill\\")` ✅ |

---

## 四、Item 纹理缺失（中等）

### 4.1 跨目录引用 ✅ 已修复

| 请求路径 | 实际位置 | 引用方式 |
|---------|---------|---------|
| `Data\Item\dkhead2.jpg` | `Data\Skill\dkhead2.OZJ` | BMD 内嵌 → `OpenTexture("Item\\")` ✅ |
| `Data\Item\dkheadwing.jpg` | `Data\Skill\dkheadwing.OZJ` | BMD 内嵌 → `OpenTexture("Item\\")` ✅ |
| `Data\Item\dkshelmet.jpg` | `Data\Skill\dkshelmet.OZJ`, `Data\NPC\dkshelmet.OZJ` | BMD 内嵌 → `OpenTexture("Item\\")` ✅ |
| `Data\Item\dksbody.tga` | `Data\Skill\dksbody.OZT`, `Data\NPC\dksbody.OZT` | BMD 内嵌 → `OpenTexture("Item\\")` ✅ |
| `Data\Item\partCharge4\monmark02a.jpg` | `ZzzOpenData.cpp:5287` `LoadBitmap("Item\\partCharge1\\monmark02a.jpg", ...)` | 路径写为 `partCharge1` 而非 `partCharge4` |
| `Data\Item\partCharge6\flareBlue.jpg` | `ZzzOpenData.cpp:5144` `LoadBitmap("Effect\\flareBlue.jpg", ...)` | 实际在 Effect 目录，代码已有正确路径 ✅ |
| `Data\Item\flareRed.jpg` | `ZzzOpenData.cpp:5152` `LoadBitmap("Effect\\flareRed.jpg", ...)` | 实际在 Effect 目录，代码已有正确路径 ✅ |

---

## 五、Interface 纹理缺失（低）

| 引用位置 | 代码 | 文件 |
|---------|------|------|
| `NewUICustomArena.cpp:54` | `LoadBitmap("Interface\\newui_btn_close.tga", IMAGE_CLOSE, ...)` | `Data\Interface\newui_btn_close.OZT` |
| `NewUICustomBuyVip.cpp:54` | `LoadBitmap("Interface\\newui_btn_close.tga", IMAGE_CUSTOM_BUYVIP_CLOSE, ...)` | 同上 |
| `NewUICustomRanking.cpp:59` | `LoadBitmap("Interface\\newui_btn_close.tga", IMAGE_CUSTOM_RANKING_CLOSE, ...)` | 同上 |
| `NewUICustomRanking.cpp:60` | `LoadBitmap("Interface\\newui_btn_minus.tga", IMAGE_CUSTOM_RANKING_MINUS, ...)` | `Data\Interface\newui_btn_minus.OZT` |

---

## 六、总计

| 类别 | 数量 | 严重程度 |
|------|------|---------|
| ExtTile 缺失 | 704 | 严重 |
| Effect 真缺失 | 2 | 中 |
| Skill 真缺失 | 2 | 中 |
| Interface 真缺失 | 2 | 低 |
| 跨目录引用 | 14（11 已自动修复） | 中 → 低 |
| **合计** | **724** | |

---

## 七、已修复的问题

| 问题 | 文件 | 说明 |
|------|------|------|
| 连接断开（FD_CLOSE 陈旧消息） | `Winmain.cpp:609-612` | 添加 `SocketClient.GetSocket() == INVALID_SOCKET` 防护 |
| Modern 协议被意外关闭 | `CSMapServer.cpp:73-87` | `ConnectChangeMapServer` 跳过 `DeleteSocket`/`CreateSocket` |
| 纹理扩展名回退 | `GlobalBitmap.cpp:407-417` | 添加 jpg↔tga AutoMatch 回退 |
| 跨目录纹理搜索 | `GlobalBitmap.cpp:418-450` | `LoadImage()` 自动搜索 Skill/Effect/Item/Interface/Player/Monster/NPC/Character 目录 |
| 日志自动清理 | `ErrorReport.cpp:84` | 取消注释 `DeleteFile` |
| SendCheck 清理 | `wsclientinline.h` | 改用直接 buffer + SendPacket |
